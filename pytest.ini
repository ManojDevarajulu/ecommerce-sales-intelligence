[pytest]
# `pythonpath = .` puts the project root on sys.path so `tests/conftest.py`
# can `from app.db.session import ...`. Without it, only a `python -m pytest`
# invocation works (that form adds the working directory itself), and a plain
# `pytest` call fails at conftest import time.
pythonpath = .
testpaths = tests
