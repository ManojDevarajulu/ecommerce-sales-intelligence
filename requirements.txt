# --- Core API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
SQLAlchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.2
pgvector==0.3.4

# --- Data processing ---
pandas==2.2.3
numpy==1.26.4

# --- Machine learning ---
scikit-learn==1.5.2
joblib==1.4.2
xgboost==3.4.1  # one of four model families compared in ml/train.py; its
                # regularized variant became the final model.

# --- LLM / RAG ---
# Embeddings are served by an Ollama instance over plain HTTP (rag/embeddings.py),
# so neither sentence-transformers nor torch is needed here.
httpx==0.27.2
tiktoken==0.8.0

# --- Utilities ---
python-dotenv==1.0.1
python-multipart==0.0.12

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Notebook (dev only) ---
jupyter==1.1.1
matplotlib==3.9.2
seaborn==0.13.2
