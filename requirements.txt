# --- Core API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
SQLAlchemy==2.0.35
psycopg[binary]==3.2.2
alembic==1.13.2
pgvector==0.3.4

# --- Data processing ---
pandas==2.2.3
numpy==1.26.4

# --- Machine learning ---
scikit-learn==1.5.2
joblib==1.4.2
xgboost==3.4.1  # added 2026-09-13: tried alongside HistGradientBoostingClassifier
                # as a 3rd model family (Random Forest also tried, rejected -
                # see ml/train.py T104/INTERVIEW_PREP.md); its regularized
                # variant became the final model.

# --- LLM / RAG ---
# Embeddings are served by an Ollama instance (rag/embeddings.py, plain HTTP via
# httpx) - sentence-transformers (and its torch dependency, ~2 GB) removed
# 2026-09-13 after the switch; see SCOPE.md change log.
httpx==0.27.2
tiktoken==0.8.0

# --- Utilities ---
python-dotenv==1.0.1
python-multipart==0.0.12

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Notebook (dev only) ---
jupyter==1.1.1
matplotlib==3.9.2
seaborn==0.13.2
